pipeline {
    /* specify nodes for executing */
    agent {
        any
    }
 
    stages {
        stage('Do the deployment') {
            steps {
                echo " Test pipeline "
            }
        }
    }
}
